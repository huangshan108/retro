<%= render 'issue_list', :is_vote => false, :is_review => true %>

<div class="current-issue-container">
  <label>Current Issue</label>
  <div class="current-issue">
    <% if @current_issue %>
      <%= @current_issue.detail %>
    <% else %>
      No More Issues!
    <% end %>
  </div>
</div>